name: Build PDFs
on:
  push:
    branches: [main]
  pull_request:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: make pdf
      - uses: actions/upload-artifact@v4
        with:
          name: executive-portfolio-pdfs
          path: output/*.pdf
