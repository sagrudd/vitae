# Publishing guide

Run `make knowledge`, `make publications`, and `make pdf`; inspect PDFs with `make render`; then use the existing `make release VERSION=<tag>` process. GitHub Pages continues to build the static site from generated content. Release assets include the PDF output directory; the dashboard and API data are generated reproducibly from the same sources.
